import { Typography } from '@mui/material';

function HomeView() {
  return (
    <main>
      <section>
        <Typography variant="h1" color="neutral.main">
          Home Section
        </Typography>
      </section>
    </main>
  );
}

export default HomeView;
